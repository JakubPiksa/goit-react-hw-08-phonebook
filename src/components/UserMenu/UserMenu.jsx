import React from 'react';

const UserMenu = () => {
  const handleLogout = () => {
    // WYLOGOWYWANIE ALE NAJPIERW NAUKA
  };

  return (
    <div>
      <p>mango@mail.com</p>
      <button onClick={handleLogout}>Logout</button>
    </div>
  );
};

export default UserMenu;
